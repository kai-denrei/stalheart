# Stalheart development log

Generated by `npm run log -- render`. Canonical entries: `docs/log/entries/`.

## 2026-09-07 — Use one numbered Sentry catalog across game and labs; retire old tower models

decision · accepted · 2026-09-07-numbered-sentry-catalog

The owner named the Sentry lab as the reference and specified radial numbering: Rotor, Plasma, Quiver, Relay, Mortar, Lancer, Howitzer, Heptapod. Impact still exposed classic weapon profiles, the range contained three extra families, and Friendly units used procedural towers without starting the authored Sentry preload. This instruction explicitly retires the classic regression roster retained during migration.

src/content/sentries.js now owns numbers 1–8, labels, model IDs, sound-cue identity and radial order. Game radial, order/upgrade HUD, codex, Sentry/Impact/Audio selectors and Friendly units share these identities. Friendly opens on Rotor, loads actual game Sentry GLBs, and previews patterns using the shared weapon kinds. Removed the classic roster, dotted/solid tower factories, head tuner and obsolete tower geometry; preserved the two organic enemy forms exactly. Removed Needle/Kiln/Railgun tiers and the old standalone Heptapod (10 GLBs). Retained 24 Sentry tier models and four Terraformer states with pinned hashes. Removed three unused sound samples, renamed five still-used generic samples without changing their bytes, and updated the audio build recipe so retired outputs cannot return. Each Sentry has an independent fire cue using the same sample as its lab reference. Existing combat values and Mortar hack gating remain unchanged. Retired roster links normalize to roster 2; unknown Sentry-family links resolve to Rotor. FX base is now stalheart-fx-2: old exports are explicitly rejected because their weapon and cue keys differ. Recreate edits in the current labs before export/promotion. Current instructions, STATE and docs/SENTRIES.md describe this baseline; historical records remain immutable.

Alternatives: Keep the classic roster and procedural tower tuner as compatibility variants: rejected by the owner’s explicit cleanup direction.; Number each UI independently: rejected because the radial, lab and unit catalog had already drifted.; Silently coerce old FX exports: rejected because retired profiles and renamed audio keys would lose or redirect edits without an explicit contract.

Evidence:

- npm test: 64 test programs passed, including exact catalog/model inventory, model loading marker, combat, assets, preset promotion and existing game rules.
- Compared all pre-cleanup Sentry combat fields against /tmp/stalheart-current-sentries.json: exact equality; organic enemy point sets also matched the pre-cleanup snapshot.
- npm run check: syntax, 14 pure-layer modules, kernel provenance, logs, FX base and all 28 pinned Sentry/Terraformer assets passed. bash -n scripts/audio-build.sh passed; samples were not re-encoded.
- Full source and nested-path release browser matrices passed: game boot, sector solvency, Sentry simulation, numbered mobile radial, input, rescue/raid, all hacking games, all three Terraformer hulls and damage transitions, labs and preset/audio round trip.
- Final release authoring matrix passed on build d6b68222: selected all eight actual GLBs in Friendly, Sentry and Impact; retired roster URL normalized; audio edits reached explicit game preview while normal game content stayed isolated; PWA controlled the release page. Artifacts: artifacts/browser-dist/.
- Browser-test failures during implementation were harness assumptions: retired roster expectations, hidden unrelated select element, lil-gui index values, duplicate radial digit badges, and same-URL navigation. Fixed selectors/navigation, removed duplicate badges, and reran successfully.
- Visual review: numbered radial screenshot and Friendly 8. Heptapod screenshot show the requested labels and authored model.
- npm run build: d6b68222, 384 files, 62,741,163 bytes. No deployment or remote push.

## 2026-09-07 — Build architecture and lab export contracts before UX and playability

decision · accepted · 2026-09-07-architecture-first-fx-contract

The owner explicitly reordered development: rebuild a strong technical foundation, refine visual and sound effects in labs with clean exports, then work on UX and playability. The inherited code had shared visual builders but mixed source-copy exports, local persistence and independent preset formats. Even the supposedly pure knob helpers imported browser storage unnecessarily.

The active roadmap and agent instructions now follow architecture → labs/export → UX → playability. Foundation v1 gives core helpers, six pure rule modules and content contracts explicit owners with compatibility re-exports. Dependency checks enforce the migrated pure layers, reject their cycles and browser dependencies, and prevent game/lab controller coupling. A strict schema-1 FX package carries all current weapon effects and 29 cue tunings; gameplay fields and sample references remain fixed. The impact lab and a new sound lab share validated import/export, isolated draft storage and explicit game preview. Runtime content is cloned and frozen once before consumers load. The promotion CLI atomically generates a reviewable shipped data module; it never deploys. The sound lab uses the real audio engine with injected definitions, separate audition preferences and disposal. Simulator records carry the selected content ID. The remaining large TD closure, shared stage/lifetime toolkit and beam/material/cinematic schema extensions are explicitly next work, not described as complete.

Alternatives: Starting with HUD changes or difficulty tuning conflicts with the owner’s requested sequence.; Moving files alone does not establish ownership. The new boundaries have executable guards, and the authoring workflow is exercised through the actual game.; Automatic application of local lab edits would make normal gameplay depend on hidden browser state. Draft preview is explicit; normal startup uses shipped content.; A broad framework/renderer rewrite would discard the current parity baseline without fixing the immediate content handoff. This phase retains native ESM and the pinned renderer/kernel.; The first browser file-import check caught an inherited navigation race: it could observe the previous page’s ready flag and query a stale DOM. The runner now clears that flag and verifies the destination URL plus readiness before interacting.

Evidence:

- docs/ARCHITECTURE.md: implemented ownership map, package contract, actual workflow, lifecycle limits and ordered remaining rebuild.
- npm test: 63 test programs passed, including negative architecture guards, preset validation/isolation and real CLI promotion in an isolated checkout.
- npm run check: 13 pure-layer modules, source identity, unchanged kernel hashes, log and 37 pinned Sentry assets passed.
- node scripts/browser-test.mjs --authoring: source file import, cross-lab draft transfer, audio audition, actual game preview and normal-game isolation passed.
- node scripts/browser-test.mjs --dist: full nested-path release matrix passed, including existing game/mission/hack/roster checks, all three A6 hulls, authoring round trip and PWA control.
- artifacts/browser-dist/lab-audio.log: decoded 29/29 cues; analyser peak 0.10332 over 50 frames. This verifies signal in Chrome, not perceived quality or real-device compatibility.
- artifacts/browser-dist/browser-fx.json and preset-preview evidence: visual impact size 0.93 and edited tower cue gain 0.42 reached actual runtime content; normal launch retained the shipped baseline.
- npm run build: b9981afe, 397 files, 63,943,375 bytes. No artistic candidate was promoted in the working repo; src/content/shipped.js still selects the baseline.
- test/promotion.mjs: export/promote/new-runtime round trip passed; repeated promotion is deterministic and invalid promotion leaves the shipped file unchanged.

## 2026-09-07 — Keep crate exits outside the A6 Terraformer footprint

issue · resolved · 2026-09-07-terraformer-camp-clearance

The player reported being stuck beside the new Stalheart immediately after deployment. Migration acceptance verified asset load and damage swaps but did not exercise manual movement from each crate. On seed 7, two legacy exits were 0.083 cell widths inside the A6 pad radius before the tank collision margin was even added.

Give the A6 look an explicit footprint available before asynchronous model loading. Use that same radius for collision and camp selection; reserve crate/steering space and sample the spherical drive-out path. The selected row can move farther from the heart. On seed 7 the three new exits clear the pad by 2.530, 1.534 and 1.416 cell widths. Changing the heart model through settings now starts a new run so physical placement is rebuilt. Browser acceptance deploys all three hulls and holds a real W key after handover; all move. The improvement backlog is a proposal with priorities and completion criteria.

Alternatives: Disabling Terraformer collision would hide the placement fault and allow driving through the machine.; Merely checking for an open graph neighbor missed physical overlap; retaining the old asset-load-only acceptance would miss this regression again.

Evidence:

- test/berths.mjs: legacy behavior retained; A6 drive-out geometry checked at 101 points per path for seeds 7, 42, 1000 and 2026.
- npm test: 60 test programs passed. npm run check: source, kernel, logs and pinned assets passed.
- npm run test:browser: all three A6 hulls clear collision and move under real forward-key input; source acceptance passed.
- node scripts/browser-test.mjs --dist: nested-path release acceptance, all-hull drive-out and PWA checks passed. Build 43d2fbdc.
- artifacts/browser/terraformer.log and artifacts/browser-dist/terraformer.log: CAMP_DRIVE movement and clearance measurements.
- docs/IMPROVEMENTS.md: proposed usability, difficulty, threat feedback, Isao orders, assets, architecture, saves and offline work.

## 2026-09-07 — Independent game migration passes source and release acceptance

change · resolved · 2026-09-07-migration-acceptance

The extraction needed to preserve a playable baseline while removing research-shell coupling, unsafe cross-app caching, ambiguous simulator outcomes and duplicated decision indexes. The first browser checks exposed a tutorial proof gate in the acceptance setup and mobile-only service-worker registration in the product.

Stalheart now has independent game, Workshop and Settings entries, canonical module identity, deterministic dist output, scoped storage/cache ownership, typed economy accounting, protected sector-breach funding, validated simulation results, local diagnostic export and an immutable development log. The browser fixture ends the tutorial before forcing a sector clear; real cold-open interaction is tested separately. PWA startup moved to the shared application entry and now installs on desktop as well as mobile. Terraformer 3000 preserves animated pivots, common damage-state scale and authored palette with a faint own-color fill for the dark board. Sixty Node test programs passed. Source acceptance and release acceptance under /stalheart/ passed, including actual sector buttons, retry, both rosters, mobile input probe, both missions, three hacks, four labs, all Terraformer states and release service-worker control. Two builds produced identical release manifests: d7606185, 380 files, 63,924,970 bytes. No remote or deployment was created.

Alternatives: Preserve the inherited wave formula during migration. Passing short bot loss baselines is not evidence that all five sectors are balanced or winnable.; Keep Terraformer 3000 opt-in until real-device performance and visual review support promotion. D0 has 78 merged meshes and 165,404 triangles; D1/D2/D3 are damage states, not performance LODs.; Use full-page route ownership to prevent hidden renderer accumulation now; extraction of the remaining combat/render/tutorial closure and sector suspend/resume remain next-phase work.

Evidence:

- npm test: 60 isolated programs passed; includes campaign solvency/accounting, storage migration, result identity, log integrity, asset animation and scoped service-worker behavior.
- npm run check: syntax, canonical source imports, unchanged kernel hashes, development log and 37 pinned Sentry assets passed.
- npm run build: d7606185; repeat build byte-identical release.json. Release payload excludes private logs, docs, tests and source audio originals.
- npm run test:browser: source acceptance passed; artifacts/browser contains screenshots, full console/network logs and sim-1.json/sim-2.json.
- node scripts/browser-test.mjs --dist: nested-path release acceptance and PWA control passed; artifacts/browser-dist contains equivalent evidence.
- Both seed-1000 style1 roster baselines end in loss at wave 3, approximately 87 simulation seconds, consistent with the research baseline; no full campaign success is claimed.
- Project deban SKILL.md passed skill-creator quick_validate.py in an isolated temporary Python environment.
- Original research: test/pwa.mjs, check-tokens.sh and export-helper syntax passed after its scoped cache and handoff edits.
- docs/MIGRATION.md and docs/STATE.md list remaining limitations: real iOS/touch/audio, campaign playtesting, difficulty tuning, monolith extraction, asset optimization, suspend/resume and complete offline installation.

## 2026-09-07 — Stalheart becomes the independently maintained game

decision · accepted · 2026-09-07-independent-stalheart

The spherical-grid research prototype grew into a game with its own asset, gameplay and release needs. The project owner requested migration into a dedicated Stalheart project, simpler robust development memory, and progressive reuse of SentryTowers_A6 assets including its Terraformer.

Preserve Git history and a pinned sphere kernel, move the game and game-facing labs into Stalheart, keep the original research checkout as its archive, and use independent entry points, storage and release output. Use one immutable validated log entry per substantive outcome instead of duplicated role indexes. Adopt Sentry assets through pinned local files and explicit adapters; integrate Terraformer 3000 as an opt-in candidate until device measurements support default promotion.

Alternatives: A clean-room rewrite would discard tested gameplay and asset contracts during an organizational move.; A second unmodified copy would retain the research shell, cache identity problems and log drift.; Hotlinking upstream models would make game behavior depend on a moving remote revision.; Changing the difficulty curve during extraction would erase the parity baseline. Keep that as the next measured experiment.

Evidence:

- docs/audit-2026-09-07/README.md and EXTRACTION.md: original audit and migration proposal
- docs/kernel-provenance.json: research commit and normalized-source hashes
- docs/sentry-assets.lock.json: 33 existing sentries match upstream; four authored Terraformer damage states pinned
- docs/STATE.md: current boundaries and next priorities
