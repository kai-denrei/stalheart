# Stalheart

Read `AGENTS.md` and `docs/STATE.md`. They are the current project instructions.
Use the project-local `/deban` skill to record or query decisions. It replaces the old four-role workflow with immutable validated entries and a generated `DEVLOG.md`.
Original private history is preserved in `.deban/legacy/`; old public working notes are in `docs/archive/`.
